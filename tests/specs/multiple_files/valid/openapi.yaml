openapi: 3.1.0
info:
  title: API for testing Flask-First
  version: 1.0.0
paths:
  /get-endpoint:
    $ref: get-endpoint.openapi.yaml#/get-endpoint
components:
  schemas:
    MessageField:
      type: string
