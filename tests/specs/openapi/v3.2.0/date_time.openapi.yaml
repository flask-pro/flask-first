openapi: 3.2.0
info:
  title: OpenAPI file for testing Flask-First.
  version: 1.0.0
paths:
  /endpoint:
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                field:
                  type: string
                  format: date-time
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                properties:
                  field:
                    type: string
                    format: date-time
