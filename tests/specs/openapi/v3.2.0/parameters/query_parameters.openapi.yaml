openapi: 3.2.0
info:
  title: OpenAPI file for testing Flask-First.
  version: 1.0.0
paths:
  /endpoint:
    parameters:
    - name: query_param_1
      in: query
      required: true
      schema:
        type: string
    get:
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                properties:
                  query_param_1:
                    type: string
