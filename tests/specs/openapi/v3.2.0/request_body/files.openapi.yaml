openapi: 3.2.0
info:
  title: OpenAPI file for testing Flask-First.
  version: 1.0.0
paths:
  /endpoint:
    post:
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '204':
          description: Empty body.
    get:
      responses:
        '200':
          content:
            application/octet-stream: {}
