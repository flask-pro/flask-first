openapi: 3.2.0
info:
  title: OpenAPI file for testing Flask-First.
  version: 1.0.0
paths:
  /endpoint:
    post:
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                properties:
                  method:
                    type: string
                    enum:
                    - POST
    get:
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                properties:
                  method:
                    type: string
                    enum:
                    - GET
