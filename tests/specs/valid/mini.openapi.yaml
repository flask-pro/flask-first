openapi: 3.0.3
info:
  title: API for testing Flask-First
  version: 1.0.0
paths:
  /get_endpoint:
    get:
      operationId: get_endpoint
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
